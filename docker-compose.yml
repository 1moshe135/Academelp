services:
  academelp:
    build: .
    image: academelp
    container_name: academelp
    init: true
    ports:
      - "${PORT:-8642}:8642"
    volumes:
      - academelp-data:/data
    restart: unless-stopped

volumes:
  academelp-data:
